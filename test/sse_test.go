package test

import (
	"testing"
)

func TestSSEServer(t *testing.T) {

	// server := sse.NewServer()
	// r := raptor.New()
	// r.GET("/events", server.Handler)
	// server.Start()
	// log.Println(server)
}
